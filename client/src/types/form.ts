export interface ICinemaForm {
    cinemaName: string;
    hallsNumber: string;
}

export interface ILoginForm {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}